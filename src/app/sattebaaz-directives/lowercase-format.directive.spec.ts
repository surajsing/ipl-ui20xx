import { LowercaseFormatDirective } from './lowercase-format.directive';

describe('LowercaseFormatDirective', () => {
  it('should create an instance', () => {
    const directive = new LowercaseFormatDirective();
    expect(directive).toBeTruthy();
  });
});
